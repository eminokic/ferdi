import { Theme } from '../../../theme';
declare const _default: (theme: Theme) => {
    message: {
        color: string;
        margin: string;
        fontSize: number;
    };
};
export default _default;
